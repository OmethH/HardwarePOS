# Optional: Set this to your live backend API URL (e.g., https://your-backend.onrender.com/api)
# When unset, Vite dev server proxies to localhost:8971 and production falls back to local sidecar.
VITE_API_URL=
